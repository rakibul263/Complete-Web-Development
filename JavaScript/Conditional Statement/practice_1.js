// create a trafic light system that show what to do based on the color of the light
let light = "red";
if(light === "red"){
    console.log("Stop the car.");
}else if(light === "yellow"){
    console.log("Ready to move.");
}else if(light === "green"){
    console.log("Move the car.");
}